spring:
  autoconfigure:
    exclude: []
  datasource:
    url: jdbc:mysql://localhost:3306/llmwiki?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: llmwiki
    password: llmwiki_2024

  ai:
    openai:
      api-key: ${AI_DASHSCOPE_API_KEY:}

llmwiki:
  storage:
    provider: local
  elasticsearch:
    uris: http://localhost:9200
    username:
    password:
  rocketmq:
    enabled: true

rocketmq:
  name-server: localhost:9876

logging:
  level:
    org.cn.liuwt.llmwiki: DEBUG
    org.apache.rocketmq: DEBUG
